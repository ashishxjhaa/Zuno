# Prefer the root Dockerfile when deploying from the GitHub repo.
# This file is only used if Railway Root Directory is set to apps/server.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bun src/index.ts"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
