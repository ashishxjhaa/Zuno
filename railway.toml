[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/server/Dockerfile"

[deploy]
# Image is built from apps/server context (see Dockerfile location)
startCommand = "bun src/index.ts"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
